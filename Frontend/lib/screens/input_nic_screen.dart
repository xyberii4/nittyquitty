import 'package:flutter/material.dart';

class InputNicScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Text('Input Nic Page', style: TextStyle(fontSize: 24, color: Colors.white));
  }
}